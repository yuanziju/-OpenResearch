# jdk.graal.compiler.runtime

## 源位置：`/opt/graal/compiler/src/jdk.graal.compiler/src/jdk/graal/compiler/runtime/`

## 状态：未开始

## 说明

编译器运行时支持：顶层仅约 1 个 .java（如 `GraalRuntime` / 错误钩子等少量类），运行时辅助。第二期范围，本期仅建框架，不预填条目；开工时由子代理列出 public 接口与 final class 全限定名后回填。

## 映射表

| Java 类（全限定名） | 职责 | 对应 Rust 文件 | 状态 | 负责人 |
|---|---|---|---|---|
