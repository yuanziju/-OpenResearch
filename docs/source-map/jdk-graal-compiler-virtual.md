# jdk.graal.compiler.virtual

## 源位置：`/opt/graal/compiler/src/jdk.graal.compiler/src/jdk/graal/compiler/virtual/`（顶层 .java 在子目录中）

## 状态：未开始

## 说明

虚拟对象逃逸分析：`VirtualState`、`VirtualUtil`、`EffectsClosure` 相关等，识别可消除分配的对象。依赖 nodes / graph / phases。第三期范围，本目录顶层 0 个 .java（类在子包）；本期仅建框架，不预填条目；开工时由子代理递归列出子包类全限定名后回填。

## 映射表

| Java 类（全限定名） | 职责 | 对应 Rust 文件 | 状态 | 负责人 |
|---|---|---|---|---|
