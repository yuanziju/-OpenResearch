# jdk.graal.compiler.nodes

## 源位置：`/opt/graal/compiler/src/jdk.graal.compiler/src/jdk/graal/compiler/nodes/`

## 状态：未开始

## 说明

`ValueNode` 体系与 HIR 节点类型（常量、参数、Phi、控制流、内存、调用等），编译器图的核心节点集。依赖 graph / core / nodeinfo / debug。第三期范围，本目录顶层约 118 个 .java；本期仅建框架，不预填条目；开工时由子代理按节点类别（calc/control/memory/cfg/extended/virtual...）分组列出全限定名后回填。

## 映射表

| Java 类（全限定名） | 职责 | 对应 Rust 文件 | 状态 | 负责人 |
|---|---|---|---|---|
