# jdk.graal.compiler.annotation

## 源位置：`/opt/graal/compiler/src/jdk.graal.compiler/src/jdk/graal/compiler/annotation/`

## 状态：未开始

## 说明

编译器内部注解：`@NodeInfo`、`@Repeatable` 辅助、`@Suppress` 等。叶子模块，无运行时依赖。第二期范围，本目录顶层约 10 个 .java；本期仅建框架，不预填条目；开工时由子代理列出注解全限定名后回填。

## 映射表

| Java 类（全限定名） | 职责 | 对应 Rust 文件 | 状态 | 负责人 |
|---|---|---|---|---|
