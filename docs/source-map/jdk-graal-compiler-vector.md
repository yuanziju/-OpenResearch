# jdk.graal.compiler.vector

## 源位置：`/opt/graal/compiler/src/jdk.graal.compiler/src/jdk/graal/compiler/vector/`（顶层 .java 在子目录中）

## 状态：未开始

## 说明

向量化与 Vector API 支持：向量节点、向量化 phase 等。依赖 nodes / graph / phases。第三期范围，本目录顶层 0 个 .java（类在子包）；本期仅建框架，不预填条目；开工时由子代理递归列出子包类全限定名后回填。

## 映射表

| Java 类（全限定名） | 职责 | 对应 Rust 文件 | 状态 | 负责人 |
|---|---|---|---|---|
