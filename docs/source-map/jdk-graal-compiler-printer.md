# jdk.graal.compiler.printer

## 源位置：`/opt/graal/compiler/src/jdk.graal.compiler/src/jdk/graal/compiler/printer/`

## 状态：未开始

## 说明

IR/图的可读化输出与 IGV 转储辅助：`GraphPrinter`、`LIRGenerator` 辅助打印、`BciBlockMapping` 可视化等。依赖 graph / nodes / graphio / lir。第三期范围，本目录顶层约 9 个 .java；本期仅建框架，不预填条目；开工时由子代理列出 public 接口与 final class 全限定名后回填。

## 映射表

| Java 类（全限定名） | 职责 | 对应 Rust 文件 | 状态 | 负责人 |
|---|---|---|---|---|
