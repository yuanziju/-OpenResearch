# jdk.graal.compiler.lir

## 源位置：`/opt/graal/compiler/src/jdk.graal.compiler/src/jdk/graal/compiler/lir/`

## 状态：未开始

## 说明

LIR（低级中间表示）指令与操作数框架：`LIRInstruction`、`Value`、`LIRGeneratorTool`、`StandardOp` 等。依赖 code / asm / graph / nodes。第三期范围，本目录顶层约 36 个 .java；本期仅建框架，不预填条目；开工时由子代理列出 public 接口与 final class 全限定名后回填。

## 映射表

| Java 类（全限定名） | 职责 | 对应 Rust 文件 | 状态 | 负责人 |
|---|---|---|---|---|
