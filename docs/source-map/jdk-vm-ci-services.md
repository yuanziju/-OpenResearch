# jdk.vm.ci.services

## 源位置：JDK25 src.zip → `jdk.internal.vm.ci` 模块，`src/jdk.internal.vm.ci/share/classes/jdk/vm/ci/services/`（不在 `/opt/graal`；共 7 文件）

## 状态：未开始

## 说明

JVMCI 服务层。提供 JVMCI 服务加载、`JVMCIServiceLocator`、模块访问等基础设施。第二期范围，本期仅建框架，不预填条目；开工时由子代理列出 public 接口与 final class 全限定名后回填。

## 映射表

| Java 类（全限定名） | 职责 | 对应 Rust 文件 | 状态 | 负责人 |
|---|---|---|---|---|
